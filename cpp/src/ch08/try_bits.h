#ifndef SRC_CH08_TRY_BITS_H_
#define SRC_CH08_TRY_BITS_H_

#include <math.h>

uint64_t floor_bit(int w, int h);
uint64_t head_missing_bit(int w, int h);
uint64_t ones(int w);
uint64_t head_filled(int w, int h);

#endif // SRC_CH08_TRY_BITS_H_
